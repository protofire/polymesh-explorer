import React from 'react';
import { Typography, Box } from '@mui/material';

export default function IdentityPage() {
  return (
    <Box m={5}>
      <Typography variant="h4">Identity page</Typography>
    </Box>
  );
}
